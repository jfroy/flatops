---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: "${APP}-kaidame-media"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ti
  volumeName: "${APP}-kaidame-media"
