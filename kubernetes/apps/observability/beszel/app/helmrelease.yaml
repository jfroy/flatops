---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/refs/heads/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: beszel
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  values:
    controllers:
      beszel:
        containers:
          beszel:
            image:
              repository: ghcr.io/henrygd/beszel/beszel
              tag: 0.18.8@sha256:99181067e5a46b3147231f456114280a8119eed371808bce79e6e7f173946975
            env:
              APP_URL: https://beszel.kantai.xyz
              DISABLE_PASSWORD_AUTH: "true"
              PORT: &port 8090
              TZ: America/Los_Angeles
            probes:
              liveness: &probe
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /api/health
                    port: *port
              readiness: *probe
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: { drop: ["ALL"] }
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
    persistence:
      data:
        existingClaim: "${APP}"
        globalMounts:
          - path: /beszel_data
      tmp:
        type: emptyDir
        medium: Memory
        sizeLimit: 1Gi
    route:
      beszel:
        hostnames:
          - beszel.kantai.xyz
        parentRefs:
          - name: envoy-internal
            namespace: network
        rules:
          - backendRefs:
              - name: beszel
                port: *port
    service:
      beszel:
        controller: beszel
        type: ClusterIP
        ports:
          http:
            port: *port
