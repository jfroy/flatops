---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mediamanager
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  driftDetection:
    mode: enabled
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    controllers:
      mediamanager:
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          init-db:
            image:
              repository: ghcr.io/home-operations/postgres-init
              tag: 18.1.0@sha256:866f15038ed5185a2b8118821f470bb7ca0df8c4231b8e277446e681ebb1ed84
            envFrom:
              - secretRef:
                  name: mediamanager-db
              - secretRef:
                  name: mediamanager-initdb
        containers:
          mediamanager:
            image:
              repository: ghcr.io/maxdorninger/mediamanager/mediamanager
              tag: 1.10.0@sha256:ba0eea0866bd95279749277e499fd15f283a6df4c121e160fa152459ec35380d
            command: ["/entrypoint.sh"]
            env:
              CONFIG_DIR: /app/config
              TZ: America/Los_Angeles
            envFrom:
              - secretRef:
                  name: mediamanager
              - secretRef:
                  name: mediamanager-db
              - secretRef:
                  name: mediamanager-auth-token
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /api/v1/health
                    port: &port 8000
                  initialDelaySeconds: 0
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness: *probes
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: {drop: ["ALL"]}
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
    persistence:
      cache:
        type: emptyDir
        sizeLimit: 1Gi
        globalMounts:
          - path: /.cache
      config:
        type: configMap
        name: mediamanager-config
        globalMounts:
          - path: /app/config
      entrypoint:
        type: configMap
        name: mediamanager-entrypoint
        defaultMode: 0755
        advancedMounts:
          mediamanager:
            mediamanager:
              - path: /entrypoint.sh
                subPath: entrypoint.sh
                readOnly: true
      media2:
        type: persistentVolumeClaim
        existingClaim: media-smb-kantai1
      metadata:
        type: persistentVolumeClaim
        size: 20Gi
        accessMode: ReadWriteOnce
        storageClass: ceph-block
    route:
      mediamanager:
        hostnames:
          - mm.kantai.xyz
        parentRefs:
          - name: envoy-internal
            namespace: network
        rules:
          - backendRefs:
              - port: *port
                name: mediamanager
    service:
      mediamanager:
        ports:
          http:
            port: *port
