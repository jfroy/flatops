---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/refs/heads/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mealie
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  values:
    controllers:
      mealie:
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          init-db:
            image:
              repository: ghcr.io/home-operations/postgres-init
              tag: 18.6.0@sha256:b6d3af974df781c673d37e49bdddfa14a6f5be28b18d2cb7713f0449bea4057b
            envFrom:
              - secretRef:
                  name: mealie-db
              - secretRef:
                  name: mealie-initdb
        containers:
          mealie:
            image:
              repository: ghcr.io/mealie-recipes/mealie
              tag: v3.24.0@sha256:0b08ac3a9f0a65b8298bcb3a9fcb596f870f92fbefbecd8fffe075fa9b6d2d5d
            env:
              ALLOW_PASSWORD_LOGIN: "true"
              ALLOW_SIGNUP: "false"
              API_PORT: &port 9000
              BASE_URL: https://mealie.kantai.xyz
              DB_ENGINE: postgres
              OIDC_ADMIN_GROUP: mealie_admins
              OIDC_AUTH_ENABLED: "true"
              OIDC_CLIENT_ID: 3409db10-3155-4d9c-bae5-f9d7eb4d02c8
              OIDC_CONFIGURATION_URL: https://pid.kantai.xyz/.well-known/openid-configuration
              OIDC_PROVIDER_NAME: Pocket ID
              OIDC_REMEMBER_ME: "true"
              OIDC_SIGNUP_ENABLED: "true"
              SMTP_AUTH_STRATEGY: NONE
              SMTP_FROM_EMAIL: mealie@kantai.xyz
              SMTP_FROM_NAME: Mealie
              SMTP_HOST: etampe.default.svc.cluster.local
              SMTP_PORT: "2525"
              TZ: America/Los_Angeles
            envFrom:
              - secretRef:
                  name: mealie
              - secretRef:
                  name: mealie-db
            probes:
              liveness: &probe
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /api/app/about
                    port: *port
              readiness: *probe
            securityContext:
              allowPrivilegeEscalation: false
              capabilities: { drop: ["ALL"] }
              readOnlyRootFilesystem: true
        pod:
          securityContext:
            runAsNonRoot: true
            runAsUser: 911
            runAsGroup: 911
            fsGroup: 911
            fsGroupChangePolicy: OnRootMismatch
    persistence:
      data:
        existingClaim: ${APP}
        advancedMounts:
          mealie:
            mealie:
              - path: /app/data
    route:
      mealie:
        hostnames:
          - mealie.kantai.xyz
        parentRefs:
          - name: envoy-internal
            namespace: network
        rules:
          - backendRefs:
              - identifier: mealie
                port: *port
            matches:
              - path:
                  type: PathPrefix
                  value: /
    service:
      mealie:
        controller: mealie
        ports:
          http:
            port: *port
