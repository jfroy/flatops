---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/refs/heads/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: browserr
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  values:
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
        fsGroupChangePolicy: OnRootMismatch
    controllers:
      browserr:
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          valkey:
            image:
              repository: ghcr.io/valkey-io/valkey
              tag: 9.1.0@sha256:4963247afc4cd33c7d3b2d2816b9f7f8eeebab148d29056c2ca4d7cbc966f2d9
            args:
              - --save
              - ""
              - --port
              - "6379"
              - --bind
              - 127.0.0.1 -::1
            restartPolicy: Always
            securityContext:
              allowPrivilegeEscalation: false
              capabilities: { drop: ["ALL"] }
              readOnlyRootFilesystem: true
        containers:
          browserr:
            image:
              repository: ghcr.io/janpuc/browserr
              tag: 0.1.0@sha256:a031dcea2b4c0320d9468744302e82551757fbb006637aff1201363e5e638ca7
            env:
              AUTH_MODE: seerr
              DEFAULT_REGION: US
              ENABLE_EMBEDDINGS: true
              ENABLE_RECOMMENDATIONS: true
              PORT: &port 3000
              REDIS_URL: redis://localhost:6379
              PUBLIC_URL: https://browserr.kantai.xyz
              SEERR_EXTERNAL_URL: https://seerr.kantai.xyz
              SEERR_INTERNAL_URL: http://seerr.default.svc.cluster.local
              SEERR_REQUEST_MODE: proxy
              TZ: America/Los_Angeles
            envFrom:
              - secretRef:
                  name: browserr
              - secretRef:
                  name: browserr-keys
            probes:
              liveness: &probe
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: *port
              readiness: *probe
            securityContext:
              allowPrivilegeEscalation: false
              capabilities: { drop: ["ALL"] }
              readOnlyRootFilesystem: true
    persistence:
      data:
        existingClaim: ${APP}
        globalMounts:
          - path: /data
      cache:
        type: emptyDir
        globalMounts:
          - path: /app/.next/cache
    route:
      browserr:
        hostnames:
          - "${APP_SUBDOMAIN:-${APP}}.kantai.xyz"
        parentRefs:
          - name: envoy-internal
            namespace: network
        rules:
          - backendRefs:
              - identifier: browserr
                port: *port
    service:
      browserr:
        controller: browserr
        ports:
          http:
            port: *port
