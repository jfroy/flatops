{
	"servers": {
		"flux-operator-mcp": {
			"type": "stdio",
			"command": "flux-operator-mcp",
			"args": [
				"serve"
			],
      "env": {
        "KUBECONFIG": "${workspaceFolder}/kubeconfig",
      },
		}
	},
	"inputs": []
}
